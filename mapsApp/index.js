/**
 * @format
 */

import { AppRegistry } from 'react-native';
import { name as appName } from './app.json';
import { MapApp } from './src/MapApp';

AppRegistry.registerComponent(appName, () => MapApp);
